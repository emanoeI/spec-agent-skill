# Cursor adapter

Install it in the target project:

```bash
spec install --cursor
```

The adapter creates:

- `.spec/`
- `.cursor/rules/spec.mdc`
- `AGENTS.md`

Restart Cursor or VS Code, then run `/spec init`. Use `/spec <request>` for later planning requests.

