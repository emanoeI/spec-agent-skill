# Codex adapter

Install it in the target project:

```bash
spec install --codex
```

The adapter creates:

- `.spec/`
- `.agents/skills/spec/`
- `AGENTS.md`

Restart Codex or VS Code, then run `$spec init`. Use `$spec <request>` for later planning requests.

