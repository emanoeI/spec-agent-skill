# Antigravity adapter

Install it in the target project:

```bash
spec install --antigravity
```

The adapter creates:

- `.spec/`
- `.agents/rules/spec.md`

Antigravity loads the rule as a workspace rule. Keep it enabled as **Always On**, then ask the agent to initialize Spec or use the Spec workflow for a planning request.
