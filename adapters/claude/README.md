# Claude Code adapter

Install it in the target project:

```bash
spec install --claude
```

The adapter creates:

- `.spec/`
- `.claude/skills/spec/`
- `CLAUDE.md`

Restart Claude Code or VS Code, then run `/spec init`. Use `/spec <request>` for later planning requests.

