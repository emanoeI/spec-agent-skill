# Generic adapter

Install it in the target project:

```bash
spec install --generic
```

The adapter creates:

- `.spec/`
- `AGENTS.md`

Restart the coding agent, then run `spec init`. Use `spec <request>` for later planning requests.

